{
  "name": "leadsure-root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "db:push": "drizzle-kit push",
    "build": "cd client && npm install && npm run build && cd .. && npm run copy-dist",
    "copy-dist": "node -e \"const fs=require('fs');const path=require('path');if(fs.existsSync('client/dist')){fs.cpSync('client/dist','dist',{recursive:true,force:true});console.log('Copied dist files');}\"",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "concurrently": "^9.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  }
}
